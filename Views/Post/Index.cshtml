@model List<ThreadShare.Models.Post>

<h1>All Posts</h1>

<ul>
    @foreach (var post in Model)
    {
        <li>@post.Title - @post.Content</li>
        <!-- Add more properties as needed -->
    }
</ul>
