@model List<ThreadShare.DTOs.Entites.PostViewModel>

<h1>All Posts</h1>

<ul>
    @foreach (var post in Model)
    {
        <li>@post.Title - @post.Body</li>
        <!-- Add more properties as needed -->
    }
</ul>
