@model List<ThreadShare.DTOs.Entites.ForumViewModel>

<h1>All Forums</h1>

<ul>
    @foreach (var forum in Model)
    {
        <li>@forum.Name - @forum.Description</li>
        <!-- Add more properties as needed -->
    }
</ul>
